# WireGuard

WireGuard support uses the official Windows `wireguard.exe` tunnel service command.

Recommended installation path:

```text
%ProgramFiles%\WireGuard\wireguard.exe
```

Portable placement is not the preferred path for WireGuard on Windows because the official app installs the required service integration.

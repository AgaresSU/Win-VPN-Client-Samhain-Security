# Samhain Security Engines

This folder is for portable protocol engines.

Recommended layout:

```text
engines/
  sing-box/
    sing-box.exe
  amneziawg/
    awg-quick.exe
```

The app also checks common system install paths and lets the user select an executable manually.

Do not commit third-party binaries here unless their license and distribution model have been reviewed.
